# DUMMY fixture sidecar secrets file for Chaperone's own test suite.
# Every credential-shaped value here is fake and inert — never a real key.

openai_api_key: sk-EXAMPLE-dummy-not-real-0000000000000000
