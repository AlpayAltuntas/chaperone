# DUMMY fixture config for Chaperone's own test suite.
# Every credential-shaped value here is fake and inert — never a real key.

llm:
  provider: anthropic
  api_key: sk-ant-api03-EXAMPLE1234567890abcdefghijklmnopqrstuvwxyz

channels:
  telegram:
    enabled: true
    bot_token: "123456789:EXAMPLE-dummy-token-not-real-AAAA11"
  whatsapp:
    enabled: false

gateway:
  host: 0.0.0.0
  port: 18789
  auth:
    token: ""
  tls: false

logging:
  level: debug
  path: ./logs/agent.log
  redact_secrets: false
  audit:
    enabled: false

trust:
  mark_untrusted_input: false
  auto_execute_links: true
  tool_allowlist: []

skills_dir: ./skills
memory_dir: ./memory
